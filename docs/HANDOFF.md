# 奇幻果园配置分享交接

## 2026-09-24 配额点数转移

- 奇幻果园顶部新增“转移配额”，显示整数起转、动态费率和向上取整规则；弹窗确认转移/到账/手续费/实付/当前/剩余六项。默认关闭由管理员开启，网络结果不明保留原请求，切页恢复不重复扣点。
- 新增严格管理员“玩家交易配额”，包含开关/费率、七项统计和完整账号历史搜索。个人中心共用20条分页交易组件，保留原流水和支付方式，增加转入/转出明细。
- 真实Edge、实际Vue/AntDesign、合成API通过关停/金额/丢失应答恢复/余额不足/分页/管理操作及390px窄屏校验；生产构建通过。未操作真实配额或部署。先部署Web API并自动迁移，再更新前端，详见[QUOTA_TRANSFER.md](QUOTA_TRANSFER.md)。


## 2026-09-23：EVT v2 迁移

保留原业务卡片和紧凑时间线，改为分页与虚拟滚动。19 项状态/历史/组件检查、生产构建和真实浏览器万条事件测试通过；阅读旧记录时新增事件不抢滚动位置。详见 [EVT-MIGRATION.md](EVT-MIGRATION.md)。

## 2026-09-23 百果争鲜支持对象改为左/右

- 支付宝和官服水果不同，页面改用selectIndex=0/1（左/右），默认左、自动选择默认关。脚本按当前区服活动配置解析ID，已有选择不切换。
- 同步类型/默认/归一化/schema；旧5301/5302按原顺序迁移，显式新字段优先、非法值关闭选择。加载/跨账号复制在补默认值前迁移，分享适配器增加可选补丁迁移回调，在过滤和合并前转换旧字段；缺字段保留接收方现值，不修改原分享对象。
- 修改前pull确认最新；256种保存加载、旧账号/新旧分享、Vue编译、10组分享回归、schema检查及生产构建通过，仅原有大包提示。与配套脚本31组活动专项共同验证跨区服和顺序变化。Web API无需改动；详见HD_REWARD_HANDOFF，需配套更新网页和脚本，保存后重启账号；未部署。用户随后授权将本次前端和脚本改动分别提交推送至各自origin/main。

## 2026-09-22 抖音添加账号扫码成功仍弹连接失败

- 提示来自 Web API 的 `/api/douyin/scan/poll` 异常响应（HTTP 502），由前端 Axios 全局错误拦截器弹出；组件本身捕获异常后仍继续轮询，因此后续成功与先前错误提示可以同时出现。Web API 查询 Python 的超时为 15 秒；本次未获得实际失败请求日志，不能确定该次异常是超时、连接故障还是上游非成功状态。
- 原添加账号页每 2 秒启动异步查询，没有在途检查；停止定时器也不会使已发请求失效，且停止函数会立即清除刚设置的绑定防重标志。离线测试已复现慢查询时同时发出三个轮询请求。
- `AddAccountModal.vue` 的抖音轮询新增每轮独立的在途检查和定时器身份校验，关闭、重新扫码及成功后的旧响应不再处理。复用现有 `handleErrorLocally`，暂时的网络/408/429/5xx 错误静默重试，连续三次失败后停止并提示一次；其他 HTTP 错误立即反馈，网站 401 仍走全局登录失效处理。
- 保留 `confirmed && game_ok` 判断和既有 `/bind` → 3001 → 选服流程；本次无需修改 Python 或 Web API。此次修复针对添加账号，已有账号重新认证流程未改动。
- 新增 `scripts/test-douyin-scan-polling.mjs`，执行实际 Vue 轮询/绑定函数与实际 Axios 全局拦截器，覆盖短暂失败恢复、慢请求、防重复绑定、连续失败、旧响应、短信和认证错误；HTTP、定时器和存储均为内存替身。
- 修改前 `git pull --ff-only` 已确认最新。扫码轮询专项、添加账号渠道回归及 `npm run build` 均通过，只有原有大包提示；本地 `dist` 已更新。用户已授权提交并推送本轮前端修复、回归脚本及交接说明；发布需更新前端构建产物，未操作真实账号，未部署。

## 2026-09-21 收获配置层级调整

- 普通水果延迟收获移动到自动收获下方并缩进；自动收获关闭时收起，分钟仅两个开关同时开启时显示，隐藏值保留。果灵独立延迟配置不变。
- 已更新现有界面回归，验证显示条件、位置、缩进、保存回读及旧分享兼容。


## 2026-09-21 普通水果延迟收获与果灵额度说明

- 修改前pull --ff-only确认最新，直接复用花园的普通延迟收获控件与失焦校验，放在自动种植下方；分钟项仅延迟开启显示。新增plant.flower.delayedHarvestEnabled=false、delayedHarvestMinutes=10，严格布尔、1-999整数，类型/默认/归一化及分享schema同步，旧配置/旧码兼容；与果灵原有延迟独立，隐藏不清空。
- 自动种果灵说明改为“今日已收获与地里已生成待收获的果灵合计达到上限时恢复原模式”。配套果园LandMgr按elRes>1统计已生成待收数量，28+2/30不再继续组合补种；普通水果和果灵各按自身延迟，不叠加，任务收获也遵守。
- 复制花园scripts/check-flower-delay-ui.mjs，改接果园现有分享适配器；真实Vue显隐/输入/失焦、保存加载、旧配置/新旧分享码和边界通过。原果灵策略专项、10组分享、schema检查及npm run build通过，仅原有大包提示；脚本13组延迟/33组果灵及土地/免费加速/竞赛回归通过，真实网络0。
- 通用Web API无需修改。按用户后续要求提交并推送本次前端与脚本改动，未部署，无实号操作；需配套更新前端及脚本，保存配置后停止再启动账号。

## 2026-09-21 公会竞赛不删有进度任务

- 修改前 `git pull --ff-only origin main` 已最新。在“删除低分任务”的子配置中新增“不删有进度的任务”，字段 `union.fmlRace.keepProgressTask`，默认false；仅删除开启时显示，隐藏保留值。与原“不接有进度”独立，不改快速设置或本人弃单策略。
- 同步GameConfig类型、默认值、严格布尔归一化及生成分享schema。旧配置默认关闭，新码保留true/false，旧码缺字段保留接收方值；Web API已通用存储，无需更新。
- 复用竞赛Vue宿主专项验证真实显隐/开关/保存加载/隐藏保留，10组分享、原竞赛配置回归、schema检查及npm run build通过（仅原有大包提示）；配套脚本以num>0在自动删除候选及POST前复核，隔离整体/接取/动作回归通过。未连接真实账号或线上服务。
- 按用户后续要求提交并推送本次前端和脚本改动，未部署；需配套更新前端及脚本，保存配置后停止再启动账号。

## 2026-09-21 适配花园种植策略与保留友情币

- 修改前 pull --ff-only，远端已最新，保留此前果灵配置待提交内容。在该基础上复用花园控件，将 clearBeforePlant 改为“全部铲完种/等待收获完成种”圆点单选，默认true，仅自动种果灵开启时显示。提示明确原铲除保护范围，等待模式按已有主果数量补空地，不等全场收完。
- 购买偷取次数下新增“保留友情币”，plant.friendSteal.friendCoinReserve默认0，范围0-9999整数，购买关闭时隐藏但保留值；示例余额205保留200最多买5次。类型/默认/归一化及分享schema同步，旧配置补0，新旧分享码按已有兼容规则导入。
- 直接复用花园 check-elves-friend-reserve-ui.mjs 并接入果园已有配置适配器，验证真实Vue显隐/双向绑定、页面保存加载、两策略、0/200/9999、非法值、旧配置默认及新旧分享码。该专项、9组分享、schema检查和npm run build通过，仅已有大包提示；脚本配套购买/土地及好友回归通过，没有实号/线上操作。
- 配套果园脚本每次及POST前读取当前权威币数与保留值，沿用num=1兑换及失败下轮重试。Web API已是通用存储，无需更新后端schema或API。需配套更新前端及脚本，保存后停止再启动账号；本次按用户要求一并提交并推送果园脚本与前端，不部署。

## 2026-09-19 修复支付宝下一步被旧白名单拦截

- 用户截图反馈选择支付宝仍提示“请选择账号密码、抖音或微信渠道”。原因是上次只恢复单选入口，handleNextStep 的 channel 分支仍只允许 0/2/3；已补充 platform=1，错误提示同步包含支付宝。修改前 pull --ff-only 确认最新，保留其他待提交果灵配置。
- 复用 scripts/test-add-account-channels.mjs，删除已过期的“支付宝隐藏”断言，执行实际页面按钮/步骤/登录/二维码函数；旧代码可复现渠道1停留在channel，修复后四个可见渠道均能进入login，非法渠道仍拒绝。验证支付宝请求二维码、成功开始轮询、失败清理loading以及扫码后转入server且不重复请求。HTTP为内存替身，没有实号扫码。
- 渠道专项、原账号密码专项及生产构建通过（仅既有大包提示）；不涉及后端或脚本改动。用户已授权将本次修复提交并推送；未部署。

## 2026-09-19 开放支付宝扫码

- 修改前 pull --ff-only，远端已最新；AddAccountModal 恢复 platform=1 支付宝入口，复用原二维码、选服和绑定流程。区服 ID 兼容 serverId/sid/id，移除包含扫码凭据的调试输出。既有 ScriptConfig/AlipayReauthModal 负责授权过期提示及重新扫码。
- 配套 qhgy-web 两处扫码授权 appId 改为 2021006190692125，果园脚本提供独立 /bind_zfb 与果园 HTTP 登录；三端需配套发布。没有新增页面配置字段或 schema。
- npm run build（vue-tsc + Vite）通过，仅已有大包提示；API 模拟绑定/重扫及脚本隔离测试通过，未实号扫码。保留此前果灵配置待提交内容；用户已授权本次支付宝改动提交并推送，未部署。

## 2026-09-19 未指定果灵的自动选择说明

- 修改前再次pull --ff-only确认最新；自动种果灵提示明确“未指定则从当期双倍名单选择主果/副果均已培育的一款”，保留默认8主其余副、自动种植/收获及每日上限说明。无可种组合回普通模式。仅文案变更，无新增字段或API；配套脚本已实现跳过未培育候选。
- 与现有铲除开关修改一并通过npm run build（vue-tsc + Vite），仅原有大包提示；差异检查通过。未提交、推送或部署。

## 2026-09-18 果灵铲除后种植开关

- 修改前工作区干净，先 pull --ff-only 快进至 f33a568，保留远端竞赛说明及选项更新。果灵的延迟收获设置下新增“铲除后种植”，字段 plant.elves.clearBeforePlant；默认 true，只有显式 false 关闭。
- 开启时由配套脚本铲除可铲除的非目标作物后补种主果和副果组合；成熟及已有果灵的地块仍按收获规则处理。关闭时等待收获，按现有主果数量补足目标，其余空地补副果。仅在自动种果灵开启时显示，关闭总开关保留原值；沿用原保存与导入流程。
- 类型、默认值、归一化和前端分享 schema 同步；通用 Web API 无改动。旧配置补默认 true，新分享可保留 false，旧码没有字段时保留接收方选择。
- 实际 Vue 开关显隐/位置/双向绑定、真实页面保存/加载函数（内存 Axios）、旧配置默认、9 组分享回归、schema --check、生产构建及 diff --check 通过；构建仅有原大包提示。未调用实号/线上 API，未提交、推送或部署。

## 2026-09-17 晚间文档：竞赛任务说明与位置

- 修改前快进至 `6fba151`，保留百果争鲜、穿丝绣锦及选择对象等远程更新。
- 将普通模式 `union.fmlRace.upgradeTask` 从任务优先级之后移到“完成已接任务”之前，名称改为“勾玉升级任务”；原消耗确认、保存字段及小号专属升级功能不变。
- `selfUpgrade` 显示名改为“自己升级过的任务”，四类接取说明同步常规页及快速设置，保留 28+1 → 57、29–99/57–99 示例并同步新选项名。文档果园段复用了花园“元宝”字样，按果园明确的“勾玉升级任务”及既有约定保留勾玉文案。
- 无配置字段、默认值、分数归一化或分享协议变化；开发完成后用户授权本次网页提交推送，不额外部署。
- 验证：竞赛规则与小号界面专项、分享 schema 检查及生产构建通过。确认移动前后的升级开关绑定与消耗确认完全一致；未做页面视觉实测。

## 2026-09-17 百果争鲜自动选择与累计点赞奖

- 修改前已pull且工作区干净。在百果争鲜新增autoSelect开关及selectFlowerId下拉：5301甜喵软憩、5302云畔萌啾。默认关闭/5301，非法值关闭自动选择，字符串数字归一化；已有游戏选择不切换。
- 原autoClaimRewards标签和说明补充累计点赞领奖（当期20/40/60/80）；不新增累计奖开关。自动点赞说明补充可手动或自动选择、缺对象/道具跳过。
- 类型、默认值、归一化及前端分享schema同步；旧码仅有autoLike/autoClaimRewards时保留接收方选择设置。复用fkssj通用schema脚本，--server-root只读校验项目元信息，不再写Web API schema/core。
- 256种开关/对象/商品组合、异常ID/false/保存重载/新旧分享码、Vue编译、8组分享、原活动回归和生产构建通过（原大包提示）。配套脚本29组隔离专项及原活动回归通过；用户随后授权本轮前端、脚本及脚本数据库更新一起提交推送至各自origin/main。未启动实号、修改Web API或部署。

## 2026-09-17 百果争鲜与穿丝绣锦

- 修改前pull快进到84caefc。活动页新增百果争鲜自动点赞/领取免费及任务奖励，穿丝绣锦自动领奖及“配合花艺上架功能就可快速完成任务”说明，全部默认关闭。字段分别为activity.flowerCompete和activity.silkEmbroidery。
- 直接复制花园ActivityShopSettings.vue/activityShop.ts，替换绣锦商店目录与勾玉文案；兑换可单独开启，默认提前5分钟（1～1440），商品加速卡/水滴/百果券/金币，余额不足所选一组转兑金币。前端价格用于展示，脚本读取当期服务器cfg。
- 复用既有默认值、类型、加载归一化、保存和分享适配器；分享schema已生成并同步qhgy-web Web API。旧码缺少新增字段时保留当前值，未修改通用分享协议或真实账号。
- scripts/test-seasonal-activities.mjs覆盖64种开关/商品组合、分钟/异常值、实际分享适配器新旧码和Vue编译；原活动/配置分享回归、两端schema一致性、Web API分享10组及生产构建通过。脚本HD13依据9月17日实包，HD23目前只有Info实包，领奖/兑换按源码模型实现；完整证据见脚本QHGY_HD_REWARD_HANDOFF。用户随后授权本轮提交推送，未部署上线。

## 2026-09-15 小号模式暂停完成已接任务

- 修改前按规范拉取，快进到 `0917705`；保留该版本已有的充值标签及配置页改动。
- 小号勾玉升级或刷新任一开启时，“完成已接任务”禁用并显示不生效说明，保留原勾选；关闭两项后恢复可编辑和普通模式保护。常规表单与快速设置复用同一模式判断和说明，修正小号总开关和两子开关中“完成已接任务优先”的旧提示。
- 仅调整现有配置页交互，无配置字段、默认值、归一化或分享协议变化；运行行为需配套更新游戏脚本。新增 `scripts/test-fml-race-small-account-ui.mjs`，与花园前端共用同一用例，直接执行各自真实页面的保存、加载和快速设置函数及 Vue 表单绑定。
- 专项界面回归、原竞赛配置回归及 `npm run build` 通过（仅既有大包提示），覆盖两开关四种组合、勾选真/假、禁用显示、保存重载、快速设置保留、逐项关闭及总开关关闭后的恢复；schema 无差异。按用户后续要求将本节界面、测试及文档提交至main并推送，配套两游戏脚本与花园前端；无账号/线上请求，未部署。

## 2026-09-15 福利配置增加平台奖励开关

- 修改前已执行 `git -c http.version=HTTP/1.1 pull --ff-only origin main`，前端已是最新。
- “基础 → 福利配置”新增“平台奖励”，字段 `basic.benefit.platformRwd` 默认 true，缺字段的旧配置保持开启；显式 false 在加载、保存及导入时保留。脚本的六项平台奖励按该开关执行，不影响其他福利。
- 复用现有 Switch、默认配置、归一化和保存接口；更新 GameConfig 类型并生成分享 schema，同步到 `qhgy-web` 的 Web API。新码支持此字段，旧码缺字段时保留当前值。
- 配置分享8组回归、`npm run build`、两端 schema 一致性及差异检查通过，包含默认值、false 保存/导入往返与旧码兼容。无真实账号或线上调用，未做浏览器截图验收。用户已授权本次提交并推送。

## 2026-09-14 复制花园分享模块

- 前端修改前已 pull --ff-only，服务端也已同步；原工作区干净。
- 新增分享入口、24小时短码、预览导入、必须确认的成功弹窗，无重新生成按钮。复用原登录、Redis/Cluster及花园现有限流，没有额外CC模块。
- 复用 normalizeGameConfigSelects 与竞赛分数校验；花市上架密码排除。
- 新增模块和协议同步脚本，文档见 CONFIG_SHARE_MANUAL.md。
- 验证：本项目前端 7 组回归（含真实页面适配器与 Vue 弹窗交互）、服务端 10 组回归、两端常规构建通过；共享代码及 core/schema 一致性检查通过。未连接线上账号/Cluster，未做真实浏览器截图验收。本次按用户要求提交并推送到 Git，未部署。
