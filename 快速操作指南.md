# 代码混淆快速操作指南

## 🎯 紧急关闭混淆（3步完成）

### 方法1: 手动修改（推荐，最快）

```bash
# 1. 编辑配置文件
vim web/build.config.js

# 2. 修改第9行
ENABLE_OBFUSCATION: false,  # 改为 false

# 3. 重新构建
cd web
npm run build

# ⏱️ 总耗时: ~1分钟（大部分是构建时间）
```

---

### 方法2: 使用脚本（Linux/Mac）

```bash
# 1. 进入web目录
cd web

# 2. 给脚本执行权限（首次）
chmod +x toggle-obfuscation.sh

# 3. 执行切换脚本
./toggle-obfuscation.sh

# 4. 重新构建
npm run build
```

---

### 方法3: Windows 快速修改

```powershell
# 1. 打开配置文件
notepad web\build.config.js

# 2. 第9行改为
ENABLE_OBFUSCATION: false,

# 3. 保存并重新构建
cd web
npm run build
```

---

## 📋 完整操作流程

### 场景1: 生产环境出现问题，需要临时关闭混淆

```bash
# === 服务器操作 ===

# 1. 进入项目目录
cd /path/to/xddq-web/web

# 2. 修改配置
vim build.config.js
# 将 ENABLE_OBFUSCATION 改为 false
# 将 REMOVE_CONSOLE 改为 false（方便看日志）

# 3. 重新构建
npm run build

# 4. 查看构建输出（确认混淆已关闭）
========================================
📦 前端构建配置:
   代码混淆: ❌ 已关闭
   混淆强度: medium
   移除Console: ❌ 否
========================================

# 5. 备份旧版本（可选）
mv /var/www/html/dist /var/www/html/dist.backup.$(date +%Y%m%d_%H%M%S)

# 6. 部署新版本
cp -r dist/* /var/www/html/

# 7. 清除浏览器缓存测试
# 按 Ctrl+Shift+R 强制刷新

# 8. 查看浏览器 Console 日志定位问题
# F12 -> Console

# ⏱️ 预计耗时: 2-3分钟
```

---

### 场景2: 问题解决后，恢复混淆

```bash
# 1. 修改配置
vim build.config.js
# 将 ENABLE_OBFUSCATION 改回 true
# 将 REMOVE_CONSOLE 改回 true

# 2. 重新构建
npm run build

# 3. 部署
cp -r dist/* /var/www/html/

# 4. 验证功能正常
```

---

## 🔧 常见配置组合

### 组合1: 正常生产环境（推荐）
```javascript
{
  ENABLE_OBFUSCATION: true,
  OBFUSCATION_LEVEL: 'medium',
  REMOVE_CONSOLE: true,
}
```
**特点**: 安全性好，性能优，文件适中

---

### 组合2: 紧急调试模式
```javascript
{
  ENABLE_OBFUSCATION: false,  // ❌ 关闭混淆
  OBFUSCATION_LEVEL: 'medium',
  REMOVE_CONSOLE: false,      // ❌ 保留日志
}
```
**特点**: 代码可读，方便排查问题

---

### 组合3: 高安全模式
```javascript
{
  ENABLE_OBFUSCATION: true,
  OBFUSCATION_LEVEL: 'high',  // ⚡ 高强度
  REMOVE_CONSOLE: true,
}
```
**特点**: 安全性最高，构建时间长

---

### 组合4: 轻混淆模式
```javascript
{
  ENABLE_OBFUSCATION: true,
  OBFUSCATION_LEVEL: 'low',   // 🚀 低强度
  REMOVE_CONSOLE: false,      // 保留日志
}
```
**特点**: 构建快，性能好，方便调试

---

## ⏱️ 各配置构建时间

| 配置 | 构建时间 | 适用场景 |
|------|---------|----------|
| 混淆关闭 | ~30秒 | 紧急修复 |
| 低强度混淆 | ~1-2分钟 | 快速发版 |
| 中等混淆（推荐） | ~3-5分钟 | 日常生产 |
| 高强度混淆 | ~5-10分钟 | 高安全要求 |

---

## 🚨 紧急情况处理SOP

### 1. 发现线上问题
```bash
# 立即关闭混淆，方便定位
vim build.config.js
# ENABLE_OBFUSCATION: false
# REMOVE_CONSOLE: false

npm run build
# 部署新版本
```

### 2. 定位问题
```bash
# 浏览器 F12 查看 Console 日志
# 查看网络请求
# 检查报错信息
```

### 3. 修复问题
```bash
# 修改源代码
# 本地测试通过
```

### 4. 恢复混淆并部署
```bash
vim build.config.js
# ENABLE_OBFUSCATION: true
# REMOVE_CONSOLE: true

npm run build
# 部署新版本
```

---

## 📞 快速参考

### 配置文件位置
```
web/build.config.js
```

### 关键配置项
```javascript
ENABLE_OBFUSCATION: true/false  // 混淆开关
OBFUSCATION_LEVEL: 'high'/'medium'/'low'  // 混淆强度
REMOVE_CONSOLE: true/false  // 移除日志
```

### 构建命令
```bash
cd web
npm run build
```

### 查看构建信息
执行 `npm run build` 时会显示当前配置

---

## ✅ 检查清单

部署前检查：
- [ ] 配置文件修改正确
- [ ] 执行了 `npm run build`
- [ ] 查看了构建输出确认配置生效
- [ ] 备份了旧版本（可选）
- [ ] 清除了浏览器缓存测试
- [ ] 验证了核心功能正常

问题解决后：
- [ ] 将混淆改回 `true`
- [ ] 将日志移除改回 `true`
- [ ] 重新构建并部署
- [ ] 验证功能正常
