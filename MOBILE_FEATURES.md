# 移动端功能说明

## 新增功能

### 1. 底部导航栏 (BottomNavBar)

- **位置**: 固定在屏幕底部
- **功能**: 三个主要页面切换
  - 🏠 首页 (配置界面)
  - 💰 充值 (充值界面)
  - 👤 我的 (个人中心)
- **特性**:
  - 毛玻璃效果背景
  - 点击动画效果
  - 活跃状态指示器
  - 支持安全区域适配 (iPhone X+)

### 2. 充值页面 (RechargePage)

- **功能**:
  - 显示当前余额
  - 预设充值金额选择
  - 自定义充值金额
  - 充值优惠显示
  - 安全提示信息
- **特性**:
  - 响应式设计
  - 动画效果
  - 深色模式支持

### 3. 页面切换动画 (PageTransition)

- **效果**: 左右滑动切换
- **性能**: 硬件加速
- **可访问性**: 支持减少动画偏好设置

### 4. 移动端适配

- **响应式断点**: 768px
- **自动检测**: 根据屏幕宽度自动切换移动/桌面模式
- **触摸优化**: 增大点击区域，优化触摸体验

## 技术实现

### 组件结构

```
Dashboard (主容器)
├── 桌面端: 传统导航栏 + 内容区域
└── 移动端:
    ├── PageTransition (页面切换动画)
    │   ├── ScriptConfig (首页)
    │   ├── RechargePage (充值)
    │   └── PersonalCenter (个人中心)
    └── BottomNavBar (底部导航)
```

### 状态管理

- `mobileActiveTab`: 当前活跃的移动端标签页
- 自动响应式检测: `window.innerWidth <= 768`

### CSS 特性

- CSS Grid 和 Flexbox 布局
- CSS 变量支持主题切换
- 硬件加速动画 (`transform`, `opacity`)
- 安全区域适配 (`env(safe-area-inset-*)`)

## 使用方式

### 开发环境测试

1. 打开浏览器开发者工具
2. 切换到移动设备模拟器
3. 刷新页面查看移动端界面
4. 测试底部导航栏切换功能

### 生产环境

- 自动检测设备类型
- 移动端显示底部导航栏
- 桌面端显示传统导航栏

## 兼容性

### 浏览器支持

- iOS Safari 12+
- Android Chrome 70+
- 现代桌面浏览器

### 设备适配

- iPhone (包括 iPhone X 系列的安全区域)
- Android 手机
- 平板设备 (768px-1024px)
- 桌面设备 (1024px+)

## 性能优化

### 动画性能

- 使用 `transform` 和 `opacity` 进行动画
- 避免触发重排和重绘
- 支持 `prefers-reduced-motion` 媒体查询

### 代码分割

- 移动端组件按需加载
- 减少初始包大小

### 内存管理

- 及时清理事件监听器
- 优化组件重渲染

## 未来扩展

### 计划功能

- 手势导航 (滑动切换页面)
- 推送通知支持
- 离线功能
- PWA 支持

### 性能优化

- 虚拟滚动
- 图片懒加载
- 服务端渲染 (SSR)
